pipeline {
  agent any

  stages {
    stage('UserInput Validation') {
      steps {
        sh '''
          echo "Executing the userinput_validation.sh"
          bash ${WORKSPACE}/linux/userinput_validate.sh
        '''
      }
    }
  }
}
