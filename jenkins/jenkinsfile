pipeline {
  agent any

  stages {
    stage('Build Stage') {
      steps {
        sh '''
          echo "Build stage is building"
        '''
      }
    }

    stage('Test Stage') {
      steps {
        sh '''
          echo "Test stage is testing"
        '''
      }
    }
  }
}
